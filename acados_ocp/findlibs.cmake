file(GLOB ACADOS_LIBS ${CMAKE_CURRENT_BINARY_DIR}/c_generated_code/*.so GLOBAL)
# print acados libs over foreach
# foreach(ACADOS_LIB ${ACADOS_LIBS})
#     message("acados lib: ${ACADOS_LIB}")
# endforeach()
